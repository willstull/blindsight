"""Blindsight CLI entry point.

The real CLI (install, describe-scenario, run-investigation, generate-report)
lands in Phase 4 of the 0.1.0 packaging work.
"""
import sys


def main() -> None:
    print("blindsight CLI: not yet implemented", file=sys.stderr)
    sys.exit(1)
